# Spec + Code-Quality Gate — `S1-04` Cross-cutting tables (audit partitioned, secrets, webhooks, billing, compliance, batches)

> **Gate worker:** pi/kimi-k2.6.  
> **IC worker:** pi/deepseek-v4-pro.  
> **Commit reviewed:** `d63dacf`.  
> **Inputs:** brief-S1-04.md, PLAN.md §S1-04, result-S1-04.txt, diff on disk, DATA_MODEL.md §10/§11/§12/§13/§15, migration 0010, smoke-S1-04.ts, artifacts, prior gates S1-01/02/03.  
> **Verdict:** 🟢 green

---

## 1. Spec adherence

### 1.1 Brief ACs 1–15

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Schema verbatim per `DATA_MODEL.md §10 §11 §12 §13` — 12 tables, columns, types, FKs, defaults, indexes | ✅ | All 12 tables present with exact column names and types. One cosmetic divergence: late-FK constraint names auto-generated by drizzle-kit include `_secrets_id_fk` suffix vs. the brief's suggested `_fk` suffix (`channels.ts:21-22`, `tools.ts:25-26`). Constraints exist and function correctly. |
| 2 | `audit_log_events` partitioning — composite PK, 3 child partitions, no duplicate CREATE TABLE | ✅ | Migration `0010_calm_betty_brant.sql:8-30` — hand-authored `CREATE TABLE ... PARTITION BY RANGE (created_at)` with `PRIMARY KEY (id, created_at)`. Three child partitions `2026_05`, `2026_06`, `2026_07` with correct inclusive/exclusive bounds. No drizzle-kit auto-emitted `CREATE TABLE audit_log_events` remains in the migration. |
| 3 | Late FK adds — `tool_catalog_providers` and `channel_connections` → `secrets(id)` | ✅ | Both constraints present in `pg_constraint` (`channel_connections_credentials_secret_id_secrets_id_fk`, `tool_catalog_providers_credentials_secret_id_secrets_id_fk`). Schema-side `.references(() => secrets.id)` added in `channels.ts:21-22` and `tools.ts:25-26`. Smoke asserts round-trip. |
| 4 | All enum-text columns get CHECK constraints (16 total) | ✅ | All 16 constraints present in DB and named correctly: `secrets_scope_check`, `webhook_deliveries_delivery_kind_check`, `audit_log_events_actor_kind_check`, `workspace_compliance_posture_{hipaa,ferpa,tcpa,eu_ai_act}_check`, `compliance_evaluations_regulation_check`, `guardrail_events_action_check`, `billing_subscriptions_{plan,status}_check`, `usage_events_kind_check`, `batches_{channel_kind,vertical,status}_check`, `batch_recipients_status_check`. Tuple values match `DATA_MODEL.md` verbatim. |
| 5 | Soft-delete columns — none on S1-04 tables | ✅ | Smoke queries `information_schema.columns` for `deleted_at` across all 12 tables; returns 0 rows. `workspace_compliance_posture` and other S1-04 tables correctly omit `deleted_at`. |
| 6 | Append-only trigger NOT applied to `audit_log_events`, `usage_events`, `webhook_deliveries`, `compliance_evaluations` | ✅ | `pg_trigger` query confirms only internal RI constraint triggers on these tables; no `BEFORE UPDATE` or `raise_append_only` trigger. Rationale documented in commit body. |
| 7 | `secrets.ciphertext` is `bytea NOT NULL` | ✅ | `secrets.ts:17` — `bytea("ciphertext").notNull()`. Migration emits `"ciphertext" "bytea" NOT NULL`. |
| 8 | `monthly_receipts` UNIQUE `(workspace_id, month)` | ✅ | `monthly_receipts_workspace_month_uidx` unique index present. Smoke asserts duplicate insertion raises unique-violation. |
| 9 | All FKs from §10/§11/§12/§13 present | ✅ | 26 FKs verified across all 12 tables including the 2 late FKs. All ON DELETE policies match spec. |
| 10 | All indexes from §10/§11/§12/§13 present | ✅ | 17 indexes verified in `pg_indexes` including DESC ordering on `webhook_deliveries(created_at)`, `audit_log_events(created_at)` ×3, `monthly_receipts(month)`, `compliance_evaluations(evaluated_at)`. |
| 11 | Migration applies cleanly | ✅ | Smoke runs against live DB migrated from S1-03 state; 37/37 pass. Journal chain `0000`→`0010` consistent. |
| 12 | Smoke runner — 37 PASS | ⚠️ partial | 37 assertions pass. Partition routing, late FKs, UNIQUE violation, and 12 of 16 CHECK constraints are exercised. **4 CHECKs are NOT tested for rejection:** `audit_log_events.actor_kind`, `workspace_compliance_posture.ferpa`, `workspace_compliance_posture.tcpa`, `workspace_compliance_posture.eu_ai_act`. The commit body over-claims "All 16 enum CHECK constraints fire correctly." |
| 13 | Type-check + lint green | ✅ | `check-types --force` 6/6 green. Lint: 0 errors, 1 pre-existing warning (`packages/env/src/web.ts`). |
| 14 | OpenAPI drift gate green | ✅ | No router changes; `gen:openapi --check` clean per commit body. |
| 15 | Demo artifacts captured | ✅ | `S1-04-partitions.txt` shows `\d+ audit_log_events` with partition layout and `\d+ audit_log_events_2026_05` with propagated indexes. `S1-04-tables.txt` shows all 12 tables. |

### 1.2 Project-specific spec gates (standing rules from gate-S1-01/02/03)

| Gate | Criterion | Status | Evidence |
|------|-----------|--------|----------|
| **A** | CHECK constraints on every new enum-text column (16 constraints) | ✅ | All 16 present and tuples match spec exactly. Verified via `pg_constraint` query. |
| **B** | Partitioned `audit_log_events` per §11:1024 | ✅ | `PARTITION BY RANGE (created_at)` on parent. Composite PK `(id, created_at)`. Three monthly children `2026-05`→`2026-07`. Partition routing proven: `INSERT ... created_at = now()` lands in `audit_log_events_2026_05` (smoke check 11). Parent indexes propagate to children (`\d+` artifact confirms). No duplicate non-partitioned `CREATE TABLE audit_log_events` in migration. |
| **C** | Late FK adds per brief AC 3 | ✅ | Both `pg_constraint` entries present. Schema-side `.references()` added to `tools.ts:25-26` and `channels.ts:21-22`. |
| **D** | Append-only trigger NOT applied | ✅ | No `BEFORE UPDATE` trigger on `audit_log_events`, `usage_events`, `webhook_deliveries`, `compliance_evaluations`. Only S1-02's `agent_versions_no_update` remains in the DB. |
| **E** | All FKs from §10/§11/§12/§13 present | ✅ | 26 FKs verified. No missing references. |
| **F** | DESC index ordering per spec | ✅ | All 5 required DESC indexes present: `webhook_deliveries(webhook_id, created_at desc)`, `audit_log_events` ×3, `monthly_receipts(workspace_id, month desc)`, `compliance_evaluations(workspace_id, regulation, evaluated_at desc)`. `usage_events(workspace_id, occurred_at)` and `(workspace_id, kind, occurred_at)` use ASC — spec does not require DESC here. |
| **G** | Soft-delete columns per §15:1196-1198 | ✅ | `information_schema.columns` query confirms zero `deleted_at` columns across all 12 S1-04 tables. |
| **H** | `secrets.ciphertext` is `bytea NOT NULL` | ✅ | Schema and migration both enforce `NOT NULL`. |
| **I** | `monthly_receipts` UNIQUE `(workspace_id, month)` | ✅ | Unique index `monthly_receipts_workspace_month_uidx` present. Smoke asserts violation. |
| **J** | All indexes from §10/§11/§12/§13 present | ✅ | 17 indexes verified in `pg_indexes`. |
| **K** | `relations()` precedent — every new table file | ✅ | All 6 new schema files declare `relations()` for every FK: `secretsRelations`, `webhooksRelations`, `webhookDeliveriesRelations`, `auditLogEventsRelations`, `billingSubscriptionsRelations`, `usageEventsRelations`, `monthlyReceiptsRelations`, `workspaceCompliancePostureRelations`, `complianceEvaluationsRelations`, `guardrailEventsRelations`, `batchesRelations`, `batchRecipientsRelations`. |
| **L** | No `catch (err: any)` in smoke runner | ✅ | `grep` returns no matches. Smoke uses `catch (err: unknown)` with `err instanceof Error` narrowing throughout. |
| **M** | Smoke runner exercises key assertions | ⚠️ partial | 37/37 pass. Exercises: late FKs ✅, partition routing ✅, monthly_receipts UNIQUE ✅, usage_events.kind CHECK ✅, compliance_evaluations.regulation CHECK ✅. **Does NOT exercise:** `audit_log_events.actor_kind` CHECK, `workspace_compliance_posture.ferpa/tcpa/eu_ai_act` CHECKs. The IC did not document which CHECKs are skipped; the commit body claims all 16 fire. |

---

## 2. File-list adherence

| Expected | Status |
|----------|--------|
| `packages/db/src/schema/secrets.ts` | ✅ created |
| `packages/db/src/schema/webhooks.ts` | ✅ created |
| `packages/db/src/schema/audit.ts` | ✅ created |
| `packages/db/src/schema/billing.ts` | ✅ created |
| `packages/db/src/schema/compliance.ts` | ✅ created |
| `packages/db/src/schema/batches.ts` | ✅ created |
| `packages/db/src/migrations/0010_*.sql` | ✅ created (`0010_calm_betty_brant.sql`) |
| `packages/db/src/migrations/meta/0010_snapshot.json` | ✅ created (6525 lines) |
| `packages/db/scripts/smoke-S1-04.ts` | ✅ created |
| `sprints/sprint-1/artifacts/S1-04-partitions.txt` | ✅ created |
| `sprints/sprint-1/artifacts/S1-04-tables.txt` | ✅ created |
| `packages/db/src/schema/index.ts` | ✅ modified (6 new re-exports) |
| `packages/db/src/migrations/meta/_journal.json` | ✅ modified (entry 0010) |
| `packages/db/src/schema/tools.ts` | ✅ modified (`.references(() => secrets.id)`) |
| `packages/db/src/schema/channels.ts` | ✅ modified (`.references(() => secrets.id)`) |

Out-of-scope edits: **none** — all 15 changed files are within `packages/db/` or `sprints/sprint-1/`. Root `package.json` and lockfile untouched.

---

## 3. Wiring + demo artifact

- **Schema index re-exports:** ✅ `packages/db/src/schema/index.ts` exports all 6 new modules alongside existing ones.
- **Migration meta journal updated:** ✅ `_journal.json` has entries `0000`→`0010` with consistent `version: "7"`.
- **`S1-04-partitions.txt`:** ✅ Shows `\d+ audit_log_events` with `Partition key: RANGE (created_at)`, three child partitions, propagated indexes on `audit_log_events_2026_05`, and CHECK/FK constraints inherited on the child.
- **`S1-04-tables.txt`:** ✅ Shows all 12 tables across 6 schema groups. Lists `audit_log_events` as `partitioned table` and children as `table`.

---

## 4. Code quality

- **`packages/db/scripts/smoke-S1-04.ts`** — 4 of 16 CHECK constraints are not exercised for rejection (`audit_log_events.actor_kind`, `workspace_compliance_posture.ferpa`, `workspace_compliance_posture.tcpa`, `workspace_compliance_posture.eu_ai_act`). The commit body over-claims "All 16 enum CHECK constraints fire correctly." **Minor** (coverage gap + disclosure issue).
- **`packages/db/src/migrations/0010_calm_betty_brant.sql`** — Late FK constraint names diverge from brief suggestion: drizzle-kit emitted `channel_connections_credentials_secret_id_secrets_id_fk` instead of `channel_connections_credentials_secret_id_fk`. Harmless; smoke queries the actual names. **Nit**.
- **`packages/db/src/schema/channels.ts:81-92`** / **`tools.ts:57-64`** — No `secret` relation added for `credentialsSecretId` in `channelConnectionsRelations` or `toolCatalogProvidersRelations`. The brief authorized only the `.references()` surgical edit, so this is within scope bounds. **Nit** (pattern drift vs. full-relations precedent).
- **`packages/db/src/schema/audit.ts`** — `inet` customType lacks `toDriver`/`fromDriver`. Same latent risk as the `vector` customType in S1-01. Acceptable at schema layer; serialization can live in repository code. **Nit**.
- **Partition DDL syntax:** Valid Postgres 15. Bounds are `FROM ('2026-05-01') TO ('2026-06-01')` — inclusive lower, exclusive upper. Correct. Partition naming is consistent (`audit_log_events_YYYY_MM`). **Clean**.
- **Composite-PK divergence:** Rationale is documented in the migration comment block (`0010:1-6`), the commit body, and the TS schema comment (`audit.ts:10-14`). Not papered over. **Clean**.
- **No `any` casts, no dead imports, no dead branches.** TS exports camelCase, SQL columns snake_case throughout. **Clean**.
- **Comments:** Near-zero in source files. Commit body is thorough. ✅

---

## 5. Honest summary

Twelve cross-cutting tables landed with correct column names, types, FK targets, ON DELETE policies, defaults, and all required indexes. The partitioned `audit_log_events` is the load-bearing feature of this story, and it is correctly implemented: composite PK `(id, created_at)`, three monthly child partitions with valid Postgres 15 range bounds, parent indexes that propagate to children, and verified partition routing via smoke. All 16 enum CHECK constraints are present in the database with exact tuples matching `DATA_MODEL.md`. Both late FKs (`tool_catalog_providers` and `channel_connections` → `secrets`) are present in `pg_constraint` and round-trip in the smoke. Type-check, lint, platform tests, and OpenAPI drift are all green. The commit body clearly documents the partition DDL approach, composite-PK divergence rationale, late-FK path, and append-only-trigger non-application rationale.

The only notable issue is a smoke coverage gap: 4 of the 16 CHECK constraints (`audit_log_events.actor_kind`, `workspace_compliance_posture.ferpa/tcpa/eu_ai_act`) are not exercised for rejection, yet the commit body claims all 16 fire. This is an overclaim, not a spec miss — the constraints do exist and would reject bad values. No other deviations from the brief or `DATA_MODEL.md` were found.

---

## 6. Recommended action

**Ready for fix-pass** (optional). The coverage gap is minor and does not block downstream stories. If the manager wants surgical perfection, the fix is a 10-line smoke expansion (see Apply-now item 1). No IC re-fire needed.

---

## 7. Apply-now items

1. **`packages/db/scripts/smoke-S1-04.ts`** — Expand CHECK coverage to exercise the 4 untested constraints. After the existing `enumTests` loop (or alongside it), add:
   ```ts
   {
     label: "audit_log_events.actor_kind CHECK",
     sql: `INSERT INTO audit_log_events (id, workspace_id, event, actor_kind, created_at)
           VALUES ('${prefix}-ae-bogus', $1, 'test', 'bogus', now())`,
     constraintName: "audit_log_events_actor_kind_check",
     params: [orgId],
   },
   {
     label: "workspace_compliance_posture.ferpa CHECK",
     sql: `INSERT INTO workspace_compliance_posture (workspace_id, hipaa, ferpa, tcpa, eu_ai_act)
           VALUES ($1, 'active', 'bogus', 'inactive', 'inactive')`,
     constraintName: "workspace_compliance_posture_ferpa_check",
     params: [orgId2],
   },
   {
     label: "workspace_compliance_posture.tcpa CHECK",
     sql: `INSERT INTO workspace_compliance_posture (workspace_id, hipaa, ferpa, tcpa, eu_ai_act)
           VALUES ($1, 'active', 'inactive', 'bogus', 'inactive')`,
     constraintName: "workspace_compliance_posture_tcpa_check",
     params: [orgId2],
   },
   {
     label: "workspace_compliance_posture.eu_ai_act CHECK",
     sql: `INSERT INTO workspace_compliance_posture (workspace_id, hipaa, ferpa, tcpa, eu_ai_act)
           VALUES ($1, 'active', 'inactive', 'inactive', 'bogus')`,
     constraintName: "workspace_compliance_posture_eu_ai_act_check",
     params: [orgId2],
   },
   ```
   (Wrap each in the same try/catch pattern used by the existing `enumTests` loop.)
