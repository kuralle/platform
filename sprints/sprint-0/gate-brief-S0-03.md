# Gate Brief — `S0-03` regen auth schema + initial migration + sign-up E2E (CODEGEN GATE)

> **You are the spec + code-quality gate worker (`pi/kimi-k2.6`). You are NOT the IC. You are NOT adversarial. You are the same team as the IC — the second pair of eyes that catches things before the manager's r1.** The IC was `pi` running on `deepseek-v4-pro`. The IC has already committed atomically. Your output is a markdown report — no code changes, no commits.

---

## 1. Context

- **Story:** `S0-03` — regen auth schema, initial migration, sign-up E2E. **THE CODEGEN GATE** (per `DATA_MODEL.md §19 step 1`).
- **IC commit:** `0200168` — `[S0-03] regen auth schema, initial migration, sign-up E2E`
- **IC worker:** `pi` / `deepseek-v4-pro`. Pi ran for ~37 min wall time; iterated heavily before committing the final clean diff.
- **You are:** `pi` / `kimi-k2.6` (a different pi worker, kimi model, performing the gate role).

**Inputs to your gate (read all of them):**
1. The story brief: `sprints/sprint-0/brief-S0-03.md`. The contract.
2. The IC's transcript: `.handoff/result-S0-03.txt` (21 lines; pi's summary). Note: pi only flushed output on completion.
3. The diff on disk:
   - `git show 0200168` for the full diff (~1651 insertions, 70 deletions across 12 files).
   - `git show --stat 0200168` for the file list.
   - Read each file the IC created or modified:
     - `packages/db/src/schema/auth.ts` (regenerated — 216 lines)
     - `packages/db/src/migrations/0000_legal_vanisher.sql` (134 lines)
     - `packages/db/src/migrations/meta/_journal.json`
     - `packages/db/src/migrations/meta/0000_snapshot.json` (978 lines — drizzle metadata)
     - `packages/db/package.json` (+`db:migrate` script, +`pg` devDep)
     - `packages/auth/src/create-kuralle-auth.ts` (S0-02 amendments — read pi's diff carefully)
     - `packages/auth/src/smoke-local.ts` (NEW — 156 lines; the sign-up E2E smoke)
     - `packages/auth/package.json` (+pg, +smoke scripts)
     - `bun.lock`
     - `sprints/sprint-0/GATE-FAIL.md` (NEW — pi's honest gate report)
     - `sprints/sprint-0/artifacts/S0-03-tables.txt`
     - `sprints/sprint-0/artifacts/S0-03-rows.txt`
4. The sprint plan for context: `sprints/sprint-0/PLAN.md` §0 (the local-Postgres-only deviation).
5. The kimi gate report on S0-02: `sprints/sprint-0/gate-S0-02.md` — **its §6 carry-forward items** for S0-03:
   - Item 1: pgEnum / CHECK constraints for enum +ext columns. **Verify whether pi added these in the migration.**
   - Item 2: verify `npx @better-auth/cli generate` runs cleanly in Node context. Pi did this — confirm.
   - Item 3: Domain-level RBAC distinguishing member from viewer. Out of scope for S0-03.
   - Item 4: Remove `as ApiKeyConfigurationOptions` cast. Pi did this in their S0-02 amendment (the cast is gone from create-kuralle-auth.ts). Verify.
6. The relevant spec docs:
   - `DATA_MODEL.md §3` (auth shapes — every `+ext` column should be present in the regenerated schema).
   - `DATA_MODEL.md §19 step 1` — **the codegen gate** ("Confirm better-auth 1.5.5 works on Cloudflare Workers runtime"). **Pi documented this as Gate-Partial.** Audit whether pi's framing is acceptable given the PLAN.md §0 pre-flight decision (local Postgres only this sprint; Neon target deferred).
7. better-auth + neon docs (use context7 if uncertain):
   - <https://www.better-auth.com/docs/concepts/cli>
   - <https://www.better-auth.com/docs/concepts/database> (databaseHooks)
8. The kickoff prompt's **rules** that apply to S0-03 specifically:
   - **§13 rule 9:** Pin latest stable. Pi's `pg` pin should match catalog. Verify.
   - **§13 rule 10:** Manager owns commits. Pi made one IC commit. ✅ structurally; verify.
   - **No shortcuts on blockers.** Zero `--no-verify`, `@ts-ignore`, `try/catch: pass`. Verify with grep on the diff.
   - **Public TS surface:** `createAuth()` signature unchanged. Verify.

---

## 2. Your job — two halves

### 2.1 Spec adherence (did the IC meet the brief?)

Walk **every** acceptance criterion in `sprints/sprint-0/brief-S0-03.md §4` (criteria 1–9). For each:
- **Met / partial / missed.** Cite file:line in the diff.
- If partial: what's missing?
- If missed: is the IC's hedge in the commit body / GATE-FAIL.md honest, or is it papering over?

**Specific things to verify rigorously:**

- **AC #1 — `auth.ts` regenerated by CLI.** The file should carry the better-auth CLI generation marker. Read the file header. The 8 better-auth tables should all be present (`user`, `session`, `account`, `verification`, `organization`, `member`, `invitation`, `apikey`). Verify each is `pgTable(...)`.
- **AC #2 — All `+ext` columns from `DATA_MODEL.md §3`** present:
  - **`user`**: `systemRole`, `lastSeenAt` ✅ if present in regenerated schema.
  - **`organization`**: `vertical`, `environment`, `region`, `isPersonal`, `createdByUserId`, `complianceMode`, `updatedAt`, `deletedAt` — verify each.
  - **`member`**: `invitedBy`, `lastActiveAt` — verify.
  - **`apikey`**: ⚠️ Per pi's S0-02 amendment, `additionalFields` was removed from apikey plugin config because the plugin doesn't accept them. Therefore `organizationId` and `revokedAt` won't be in the generated schema for apikey. Pi mapped `organizationId` to the auto-generated `referenceId`. The `revokedAt` column is genuinely missing. **Audit:**
     - Is the apiKey plugin's `references: "organization"` setting actually generating `referenceId` as the org FK? Read the migration SQL — confirm.
     - Is the missing `revokedAt` a real spec deviation that should land in `sprints/AMENDMENT-002.md`? **The manager (main session) was planning to write AMENDMENT-002 for this.** Surface the need explicitly.
- **AC #3 — Initial migration `0000_*.sql`** emitted. Read it. Verify it creates all 8 tables with correct FKs and indexes. Verify it doesn't reference any `pgEnum` (cf. kimi's S0-02 gate report §6 carry-forward 1: enum columns are `text` because better-auth's CLI doesn't emit `pgEnum`). **Important:** kimi predicted that pgEnum/CHECK constraints would need to be added in the migration. **Did pi add them?** Probably not (pi's commit message doesn't mention them). Flag this as a S0-fix-pass item: enum columns are stored as `text` without `CHECK` constraints, so any `'production' | 'staging' | 'sandbox'` value at the application layer can be violated by direct SQL writes.
- **AC #4 — `db:migrate` succeeds** against `kuralle_dev`. Pi added the `db:migrate` script to `packages/db/package.json`. Verify the script exists. Pi's `S0-03-tables.txt` artifact captures the post-migrate `\dt` showing 8 tables.
- **AC #5 — Sign-up E2E.** Pi proved this against local Postgres + `pg` driver (NOT against Neon-HTTP, NOT through wrangler-dev). The four expected rows are documented in `S0-03-rows.txt`. **Audit:**
   - Are all four assertions actually verified in the artifact? (user with email, organization with isPersonal=true and createdByUserId, member with role=owner, session with active_organization_id matching org.id.)
   - Pi placed the smoke at `packages/auth/src/smoke-local.ts` instead of the brief's `scripts/sprint-0/signup-smoke.ts`. **Audit the deviation:** does it matter? Functionally the smoke runs the same gate; the location is different. Manager's view: tracked-package location is more durable (matches what survived pi's ~37-min iteration loop). Reasonable.
- **AC #6 — Demo artifact `S0-03-signup.{cast,mp4}`.** Pi did NOT capture this. Pi's commit body says: "Didn't create demo screencast — the existing A1 sign-in UI is mock-only…wiring the sign-in screen to better-auth's sign-up endpoint is out of scope." **Audit:** is this a justified deferral or scope creep? The brief required it. The hedge is honest but the artifact is missing. Flag as `Apply now` for manager fix-pass: either capture an asciinema cast of the smoke run (proves the same thing programmatically) OR formally defer with a backlog entry.
- **AC #7 — `bun run check-types` green workspace-wide.** Verify by running `bun run check-types` against HEAD.
- **AC #8 — No hand edits to regenerated `auth.ts`.** Verify by reading the file's header (CLI marker should be intact) and checking pi's commit body for any "I edited the regenerated file" disclosure.
- **AC #9 — GATE failure handling.** Pi did **not** stop and fail the gate. Pi committed AND wrote `GATE-FAIL.md` documenting the partial state. **Audit:** is "Gate-Partial" an honest framing, or is it a smuggling-in of a failure that should have triggered AC #9's "stop and write GATE-FAIL.md without committing"?
  - The brief AC #9 says: "if sign-up E2E fails on Workers + Neon-HTTP + better-auth, **stop and flag**…do not commit a half-working state."
  - Sign-up E2E **did not fail**. It was **never tested** against Workers + Neon-HTTP because the test environment doesn't allow it (no Neon DB, no CF credentials).
  - Sign-up E2E **passed** against local Postgres + pg. The schema and hooks are validated.
  - PLAN.md §0 explicitly chose "local Postgres only this sprint; Neon target deferred."
  - **Verdict:** pi's framing is honest. The codegen-gate as written ("Confirm better-auth 1.5.5 works on Cloudflare Workers runtime") strictly speaking was NOT proven, but the load-bearing claim (better-auth + drizzle + Postgres + hooks all wire correctly) IS proven. Pi's GATE-FAIL.md correctly surfaces the gap and offers four paths forward. The user's pre-flight decision (PLAN.md §0) already accepted this trade-off.
  - **Action:** flag pi's interpretation for r1 review; recommend manager accept Gate-Partial as in-line with PLAN.md §0.

### 2.2 Code quality

For every new or modified source file, check (per the gate template §2.2):

- **`packages/auth/src/create-kuralle-auth.ts` diff (S0-02 amendments).** Pi made two changes:
  1. Removed `apikey.additionalFields` from `apiKeyPluginOptions` and dropped the `as ApiKeyConfigurationOptions` cast. **Audit:**
     - Is the new shape (`{ references: "organization" as const }`) correct for the apiKey plugin? Verify against `@better-auth/api-key@1.5.5` source.
     - Did pi remove the `ApiKeyConfigurationOptions` import? Verify.
  2. Changed `session.create.before` → `session.create.after`. Reason: transaction isolation made the personal org invisible from `.before`. **Audit:**
     - Is `session.create.after` documented in better-auth 1.5.5? Does the API surface match? (You can verify against `node_modules/better-auth/dist/types.d.mts` or via context7.)
     - Does the `is_personal || isPersonal` snake_case fallback still exist after the move? If so, is it necessary or a vestige? (It might still be needed because drizzle-pg returns snake_case columns by default; better-auth's mapping of additionalFields may not auto-camelCase the response row.) Read pi's diff carefully.
- **`packages/db/src/schema/auth.ts`** (regenerated).
  - Header has CLI generation marker.
  - All 8 tables present.
  - All `+ext` columns present (per AC #2 audit above).
  - `pgTable`, `pgcore.text`, `pgcore.timestamp`, `pgcore.boolean` types — all valid.
- **`packages/db/src/migrations/0000_legal_vanisher.sql`** (134 lines).
  - Read the SQL once. Look for SQL-injection patterns (none expected; this is auto-generated).
  - All FKs declared.
  - All indexes declared.
  - **No `CREATE TYPE`** statements (no pgEnum). This confirms enum columns are `text`. Flag as a fix-pass item.
- **`packages/auth/src/smoke-local.ts`** (156 lines, NEW).
  - Read the full file.
  - **No `--no-verify`, no `@ts-ignore`, no `try/catch: pass`.** Verify.
  - Imports use `import type` where possible.
  - The smoke connects to `pg`, runs sign-up via better-auth's API directly (NOT via HTTP), and verifies the four rows in Postgres. This is more direct than HTTP-based; it's actually a stronger gate.
  - Naming, type tightness, error handling.
  - Fail-fast on assertion violation.
  - Writes the artifact deterministically.
- **`packages/db/package.json`** + **`packages/auth/package.json`**:
  - `pg: catalog:` added (already in catalog from S0-01).
  - New scripts: `db:migrate` (db), `smoke-local` (auth). Verify they're correct.
- **`bun.lock`** — only the diff for the new pg-as-direct-dep edges. Should be small.
- **`sprints/sprint-0/GATE-FAIL.md`** — pi's gate report. Read it. Audit:
  - Is the framing honest?
  - Are the four paths forward correctly characterized?
  - Should this file be renamed to `GATE-PARTIAL.md` (to match pi's actual verdict in the file body)? `GATE-FAIL` is alarmist if the actual verdict is `Gate-Partial`. Flag for r1.
- **Test quality.**
  - The smoke-local.ts is a behavioral test for the auth package. It exercises sign-up + hooks + DB writes end-to-end. Sufficient for AC #5.
  - No fake placeholder tests.
- **Project-specific rules:**
  - **OpenAPI drift CI** (kickoff §13 rule 11): S0-03 doesn't add oRPC routers. ✅ N/A.
  - **Hook-wrapper rule** (kickoff §13 rule 12): S0-03 doesn't touch `apps/web`. ✅ N/A.
  - **Hexagonal discipline** (kickoff §13 rule 13): `core/`/`runtime/` don't exist yet. `auth/` is its own module. The smoke uses `pg.Client` directly inside `packages/auth/src/smoke-local.ts` — that's a script, not domain code; the rule doesn't strictly forbid it. But it's worth noting: production `auth/` should NOT depend on `pg` directly — only via the `db` parameter in `createKuralleBetterAuth`. **Verify** that `create-kuralle-auth.ts` itself doesn't import `pg` (only `smoke-local.ts` does, and it's a side-script).

---

## 3. Output

Write `sprints/sprint-0/gate-S0-03.md` with these sections:

```md
# Spec + Code-Quality Gate — `S0-03` regen auth schema + sign-up E2E

> **Gate worker:** pi / kimi-k2.6.
> **IC worker:** pi / deepseek-v4-pro.
> **Verdict:** {green | yellow | red}

## 1. Spec adherence
(table)

## 2. File-list adherence
(table)

## 3. The S0-02 amendments — audit
- session.create.before → after
- apikey config simplification

## 4. The Gate-Partial framing — audit
Honest? Justified by PLAN.md §0? Or smuggled?

## 5. Code quality
(bullets)

## 6. Apply now items for manager fix pass
1. ...
2. ...
3. ...

## 7. Carry-forwards
- Add CHECK or pgEnum constraints for enum +ext columns? In which migration?
- AMENDMENT-002 needed for apikey divergence (referenceId IS organizationId; revokedAt deferred)?
- Capture S0-03 demo artifact (asciinema of smoke run) or formally defer?

## 8. Honest summary

## 9. Recommended action
- Ready for r1
- Needs IC fix pass before r1
- Ambiguous spec — manager owns
```

Verdict: green | yellow | red.

---

## 4. What NOT to do

- Do not rewrite the IC's code.
- Do not be adversarial. r2 (codex) is the adversarial pass — that's not you.
- Do not duplicate brief language.
- Do not skip files.
- **Do not commit.** Manager owns commits.
- Do not approve a story with fake artifacts or misleading commit-body claims.
- **When in doubt about better-auth API shape, use context7** (`/better-auth/better-auth`).

---

## 5. Tone

Calm, grounded, on-team. The manager reads your report alongside the diff, alongside `GATE-FAIL.md`. Your report should make the manager's r1 review faster, not redundant.
